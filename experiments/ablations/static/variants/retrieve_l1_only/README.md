# retrieve_l1_only

Question: how much of the gain comes from low-level transferable experiences?

This reuses an existing full static tree and exports only L1 ExperienceCards.
Set `BASELINE_TREE_DIR` to the full baseline `dynamix_tree/` directory, plus
`RECORDS_PATH` or `REUSE_TRAIN_RUN_DIR` to reuse train artifacts.
