# retrieve_all_cards

Re-export all ExperienceCards from an existing unfiltered full static tree. This
is a retrieval-level ablation: it must use the same `REUSE_TREE_DIR` as
`retrieve_l1_only` and `retrieve_l2plus_only`, so the three variants differ only
by nodebank level filtering.
