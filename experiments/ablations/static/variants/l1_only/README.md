# l1_only

Question: does higher-level abstraction beyond L1 help transfer?

This builds only one hierarchy step, L0 -> L1, and exports only L1 cards. It is
a build ablation, not merely a retrieval filter.
