# retrieve_l2plus_only

Question: how much of the gain comes from high-level abstractions?

This reuses an existing full static tree and exports only L2+ ExperienceCards.
Set `BASELINE_TREE_DIR` to the full baseline `dynamix_tree/` directory, plus
`RECORDS_PATH` or `REUSE_TRAIN_RUN_DIR` to reuse train artifacts.
