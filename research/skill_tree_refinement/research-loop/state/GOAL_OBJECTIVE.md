# Goal Objective

Advance the idea-led research loop whose source of truth is
`/mnt/data/yaodong/codes/DynaMix2skill/research/skill_tree_refinement/research-loop/idea.md`
and whose workspace is
`/mnt/data/yaodong/codes/DynaMix2skill/research/skill_tree_refinement/research-loop`.

Current phase: Evidence-to-Idea Refinement Gate. First produce a source-verified
literature/closest-work map and a fair audit of existing ablations, then freeze
the IDEA-001 global-granularity experiment contract. Use Codebase-Memory for
repo-level impact, Serena for live symbol paths, and Ponytail to implement only
the smallest winning mechanism. Preserve the current SpreadsheetBench split,
LibreOffice evaluator, Qwen3.5-9B-AWQ endpoint, Qwen3-Embedding-8B settings,
thinking/decoding, tools, top-k, max turns, records, denominator, and retry/cache
policy unless an explicit reviewed amendment is made.

Run the smallest credible diagnostics first. Up to 32 workers are authorized.
Every implementation must follow governed development and receive one
independent spec/research-protocol reviewer before serious runs. Save complete
intent, command, config, logs, token/runtime, metrics, task flips, and analysis.

Pause for user input before changing the formal split/evaluator/model endpoint,
tool or information access, retrieval/index semantics, headline claim,
overwriting baseline artifacts, mixing into existing run dirs, committing
unrelated dirty changes, or exceeding one matched full 200-train/200-heldout
campaign. Do not silently commit or push the current dirty worktree.
